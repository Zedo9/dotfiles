#!/bin/sh

# Get user selection via dmenu
chosen=$(cut -d ';' -f1 ~/dotfiles/.utils/chars/* | dmenu -i -l 30 | sed "s/ .*//")

# Exit if none chosen
[ -z "$chosen" ] && exit

printf "$chosen" | xclip -selection clipboard
notify-send "'$chosen' copied to clipboard." &